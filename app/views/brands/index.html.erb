<p id="notice"><%= notice %></p>

<h1>Marcas</h1>

<table class="table">
  <thead>
    <tr>
      <th class="text-center">Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @brands.each do |brand| %>
      <tr>
        <td><%= brand.name %></td>
        <td><%= link_to 'Editar', edit_brand_path(brand), :class => 'btn btn-default' %></td>
        <td><%= link_to 'Eliminar', brand, method: :delete, data: { confirm: '¿Estas seguro?' }, :class => 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Marca', new_brand_path, :class => 'btn btn-primary'%>
