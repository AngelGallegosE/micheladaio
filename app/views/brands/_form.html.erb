<%= simple_form_for(@brand, html: { class:'form-horizontal' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label:'Nombre' %>
  </div>

  <div class="form-actions">
    <%= f.submit 'Guardar', :class => 'btn btn-primary'  %>
  </div>
<% end %>
